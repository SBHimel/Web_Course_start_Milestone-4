import { Button } from "@heroui/react";

const HeroPage = () => {
    return (
        <div>
            <h2>Hero page</h2>
            <Button variant="danger-soft">Hero Button</Button>
        </div>
    );
};

export default HeroPage;